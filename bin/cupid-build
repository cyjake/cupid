#!/usr/bin/env node

var cupid = require('../')
var utils = require('../lib/utils')


process.stdin.resume()

cupid.build('alibaba')
  // .fail(function(err) { utils.error(err) })
  .done(function(data) {
    utils.log('done', 'build ' + data.title + ' succeed')
    process.exit(0)
  })